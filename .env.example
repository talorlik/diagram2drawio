# Copy to .env and fill in the key(s) you use.
# .env is gitignored — never commit real secrets.
#
# Only the key for your --ai-provider is required for image inputs.
# Mermaid (.mmd) and PlantUML (.puml) conversion needs no API key.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
